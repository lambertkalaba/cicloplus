// Este archivo quedó huérfano: `HomeScreen` no se instancia en ningún tab
// de `MainTabScreen` (el calendario real vive en `calendar_screen.dart`).
// Se vació para no seguir cargando el editor de día antiguo (DayEditorSheet)
// en el bundle sin necesidad — si se vuelve a necesitar esta pantalla,
// reconstruir usando `calendar_screen.dart` como referencia.
