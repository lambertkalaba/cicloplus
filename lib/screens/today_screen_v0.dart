// Prototipo huérfano: este archivo nunca se importa ni se instancia en
// ningún lugar de la app (confirmado por auditoría previa al lanzamiento
// en Play Store/App Store). La pestaña "Hoy" real vive en
// lib/screens/today_screen.dart, usada por MainTabScreen. Se vació este
// archivo para no seguir cargando código sin uso (incluía botones sin
// implementar) en el bundle. Si hiciera falta revisar el diseño original,
// buscar en el historial de versiones.
